apiVersion: apps/v1
kind: Deployment
metadata:
  name: account-db
  namespace: banking-system
  labels:
    app: account-db
spec:
  replicas: 1
  selector:
    matchLabels:
      app: account-db
  template:
    metadata:
      labels:
        app: account-db
    spec:
      containers:
        - name: postgres
          image: postgres:15
          ports:
            - containerPort: 5432
          env:
            - name: POSTGRES_DB
              value: "account_db"
            - name: POSTGRES_USER
              value: "postgres"
            - name: POSTGRES_PASSWORD
              value: "postgres"
---
apiVersion: v1
kind: Service
metadata:
  name: account-db-service
  namespace: banking-system
spec:
  selector:
    app: account-db
  ports:
    - protocol: TCP
      port: 5432
      targetPort: 5432